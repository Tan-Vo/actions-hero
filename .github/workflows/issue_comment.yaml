name: create a comment on new issue

on:
  issues:
    types: [opened]
    
jobs:
  comment-with-actions:
    runs-on: ubuntu-latest
    steps:
      - name: "define github context"
        run: echo '${{ toJSON(github.event) }}' | jq
        shell: bash 
